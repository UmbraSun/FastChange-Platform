import { RegisterForm } from "@/features/auth/ui/RegisterForm";

export default function RegisterPage() {
  return (
    <main className="min-h-screen bg-exchange-bg px-4 py-12 text-exchange-text">
      <RegisterForm />
    </main>
  );
}